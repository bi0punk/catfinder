services:
  catfinder:
    build: .
    container_name: catfinder_mvp
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./config:/app/config
      - ./captures:/app/captures
      - ./models:/app/models:ro
    shm_size: "512mb"
